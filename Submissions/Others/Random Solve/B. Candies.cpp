#include <bits/stdc++.h>
using namespace std;

long long n;
void solve() {
    cin >> n;

}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int t = 1;
    cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}