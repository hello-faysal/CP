#include <bits/stdc++.h>
using namespace std;

int32_t main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);

  int w, h; cin >> w >> h;
  if (60 <= w and 130 >= h) {
    cout << "YES\n";
  }
  else {
    cout << "NO\n";
  }

  return 0;
}